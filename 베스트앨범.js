/**
 장르의 누적값을 기록할 sums
 각 장르의 수록곡 배열을 값으로 가지는 lists

 */

/**
 *
 * @param {string[]} genres
 * @param {number[]} plays
 * @returns
 */
function solution(genres, plays) {
  var answer = [];
  return answer;
}

solution(
  ["classic", "pop", "classic", "classic", "pop"],
  [500, 600, 150, 800, 2500]
);
